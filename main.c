#include "monty.h"

/**
 * main - monty interpreter
 * @argc: args counter
 * @argv: args values
 * Return: exit status
 */
int main(int argc, char *argv[])
{
  /* validate path */

  if (argc != 2)
    perror_exit(0, 0, NULL, NULL);
      /* perror_exit */

  argv = argv;
  /* initialize stack */
  /* read file*/
      /* execute */
  /*free mallocs */
  return (EXIT_SUCCESS);
}
